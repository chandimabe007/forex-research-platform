__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.ruff_cache/

# Credentials never in the repository (SEC-001). .env is local development only.
.env

# Data directories: venue capture, ticks, probe records, manifests of local
# runs. Anchored to the repo root — an unanchored `data/` would also ignore
# src/forex_research/data/, the package's own subpackage (this actually
# happened: CI caught the missing modules on the first push).
/data/
.venv-ci/
